services:
  textffcut:
    build: .
    image: textffcut:0.9.3
    container_name: textffcut_app
    restart: no  # 自動再起動しない
    ports:
      - "8501:8501"
    volumes:
      - ./videos:/app/videos
      - ./output:/app/output
      - ./transcriptions:/app/transcriptions
      - ./logs:/app/logs
      - ./models:/home/appuser/.cache
    environment:
      - TZ=Asia/Tokyo
      - HOST_VIDEOS_PATH=${PWD}/videos