# Whisper LoRA PoC 用の独立依存
# 本体 TextffCut の requirements.txt とは分離する
# （torch/transformers のバージョン衝突防止のため、専用 venv で使うこと）

# PyTorch (MPS バックエンド対応)
torch>=2.3.0
torchaudio>=2.3.0

# Transformers + PEFT (LoRA)
# transformers 5.x は PEFT + Whisper seq2seq forward と API 衝突するため 4.x に固定
transformers>=4.44.0,<5.0
peft>=0.11.0,<0.16
accelerate>=0.30.0

# データ処理
datasets>=2.18.0
librosa>=0.10.0
soundfile>=0.12.0

# 評価指標
evaluate>=0.4.0
jiwer>=3.0.0

# ユーティリティ
pyyaml>=6.0
tqdm>=4.66.0
